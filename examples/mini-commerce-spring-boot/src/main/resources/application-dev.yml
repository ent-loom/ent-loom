spring:
  datasource:
    url: jdbc:mysql://${DEV_MYSQL_HOST:localhost}:${DEV_MYSQL_PORT:3306}/${DEV_MYSQL_DATABASE:mini_commerce}?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Shanghai
    username: ${DEV_MYSQL_USER:mini_commerce}
    password: ${DEV_MYSQL_PASSWORD:mini_commerce_dev}
  sql:
    init:
      mode: always

server:
  port: ${DEV_APP_PORT:8082}
