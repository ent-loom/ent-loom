spring:
  datasource:
    url: jdbc:mysql://${DEV_MYSQL_HOST:localhost}:${DEV_MYSQL_PORT:3306}/${DEV_MYSQL_DATABASE:customer_profile}?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Shanghai
    username: ${DEV_MYSQL_USER:customer_profile}
    password: ${DEV_MYSQL_PASSWORD:customer_profile_dev}
  sql:
    init:
      mode: always

server:
  port: ${DEV_APP_PORT:8080}
