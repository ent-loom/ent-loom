# 当前实施总览

> 状态：Completed
> 最近核验：2026-08-21

本页只提供跨领域执行顺序，不复制各计划的验收细节。需要逐项执行时，使用[框架实施清单](实施清单.md)。

## 当前主线

当前无进行中闭环。CRUD `C1-C7`、Meta `M1-M4` 均已完成，验收证据统一维护在[框架实施清单](实施清单.md)。

## 下一阶段候选

- CRUD：完整异步 Worker 调度、快照过期和重新授权策略，详见[CRUD 重构路线](crud/CRUD重构路线.md)。
- Core：Java/Spring 兼容线仍暂不启动，详见[Java 与 Spring 兼容性路线图](core/Java与Spring兼容性路线.md)。
- DDL、DOC、UI 和公共 SPI：继续等待稳定 Runtime Model、真实调用者和消费者闭环。

## 已完成阶段顺序

已完成阶段的历史顺序保留在各领域路线图，不作为当前执行队列。

## 更新规则

一个闭环完成后：

1. 更新 Architecture 中的当前事实。
2. 从详细路线删除已完成正文。
3. 将本页下一项提升为当前项。
4. 只有存在长期取舍时才新增 Decision。
