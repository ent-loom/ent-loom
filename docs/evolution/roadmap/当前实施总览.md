# 当前实施总览

> 状态：In Progress
> 最近核验：2026-08-25

本页只提供跨领域执行顺序，不复制各计划的验收细节。需要逐项执行时，使用[框架实施清单](实施清单.md)。

## 当前主线

当前主线为 DDL 实体能力闭环，执行顺序为 `E1 -> E2 -> E3 -> E4 -> E5`，详细清单见 [DDL 实施清单](ddl/DDL实施清单.md)。

当前小项：`E5`，实体全链路验收（待启动）。

CRUD `C1-C7`、Meta `M1-M4` 已完成，作为 DDL 主线的既有能力基线；验收证据统一维护在[框架实施清单](实施清单.md)。

## 后续阶段候选

- DDL：E5 实体全链路验收。
- CRUD：完整异步 Worker 调度、快照过期和重新授权策略，继续暂缓，详见[CRUD 重构路线](crud/CRUD重构路线.md)。
- Core：Java/Spring 兼容线继续暂不启动，详见[Java 与 Spring 兼容性路线图](core/Java与Spring兼容性路线.md)。

## 已完成阶段顺序

已完成阶段的历史顺序保留在各领域路线图，不作为当前执行队列。

## 更新规则

一个闭环完成后：

1. 更新 Architecture 中的当前事实。
2. 从详细路线删除已完成正文。
3. 将本页下一项提升为当前项；DDL 阶段状态以 [DDL 实施清单](ddl/DDL实施清单.md) 为准。
4. 只有存在长期取舍时才新增 Decision。
