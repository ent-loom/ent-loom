/**
 * Runtime entrypoints shared by all CRUD capabilities.
 *
 * <p>This package is the migration target for execution context, route keys,
 * scene registration, route resolution, and metadata entrypoints.</p>
 */
package com.entloom.crud.core.runtime;
